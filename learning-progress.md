# 我的学习进度

> 记录个人推进轨迹、阶段状态与关键领悟。根目录 `README.md` 只保留项目导览和当前状态摘要。

## 当前状态

- 第 0 阶段：第 1 课进行中
- 第一阶段：三课已完成
- 第二阶段：第 5 课（从零搭建 GPT，首次进入 PyTorch）进行中——主课已跑通，自写练习 `05_gpt_from_scratch_self_write.py`（10 个 TODO）已搭好待填
- 第三阶段：待学习
- 第四阶段（DeepSeek 与推理优化）：课程已搭建，待学习
- 第五阶段（Agent 架构与 Agent 基础设施）：课程已搭建，面向 Agent infra 方向，待学习

## 进展记录

- 2026-04-14：完成第一阶段第 3 课（神经网络前向/反向传播与 XOR 分类）自写练习，并已重置练习文件准备二刷。
- 2026-04-18：完成第二阶段第 1 课（词嵌入与位置编码）。跑通 `01_word_embeddings.py`、吃透正弦位置编码公式与 `div_term` 的 `exp(log)` 写法，看懂位置编码热力图/曲线图，完成自写练习 `01_word_embeddings_self_write.py`，并在 [phase2-transformer/README.md](phase2-transformer/README.md) 沉淀了"相加不是无损拼接，而是低成本注入顺序信息"和"放大镜 + 望远镜（小维度高频看近处、大维度低频看远处）"等 6 点关键领悟。可用 `reset_exercises_01.py` 重置练习准备二刷。
- 2026-04-18：补充 GPT-1 论文 PDF（`papers/core-transformers/GPT1_2018_improving_language_understanding.pdf`），并整理 [papers/notes/notes_gpt2_input_and_model.md](papers/notes/notes_gpt2_input_and_model.md) 笔记，配合第 1 课对照理解 decoder-only 的输入表示。
- 2026-04-25：完成第二阶段第 2 课（自注意力机制）。跑通 `02_self_attention.py` 并按 8 个 TODO 手写 `02_self_attention_self_write.py`：数值稳定 softmax -> Q/K/V 投影 -> scores -> scale -> softmax -> 加权 -> `self_attention` 函数封装 -> 因果掩码 `np.triu` -> `causal_self_attention`（GPT 用），全部校验通过。
- 2026-04-25：开始第二阶段第 3 课（多头注意力 / 残差连接 / LayerNorm）。在 [phase2-transformer/README.md](phase2-transformer/README.md) 补充了李宏毅 Multi-Head Attention 中文视频，按"看视频 -> 读论文 -> 跑 `03_multi_head_attention.py` -> 改 `n_heads`/去残差实验"的节奏推进。
- 2026-04-26：开始第 0 阶段第 1 课（向量、形状、axis、广播）。把 `01_vectors_and_axes.py` 的 axis 注释改成"聚合方向法"，给广播规则补了 5 条 Q&A（为什么从右对齐 / 为什么补 1 / 为什么不静默通过 / 不真复制内存 / 任意维度都成立），修正错误示范并加 `(3,4)` 与 `reshape(2,1,4)` 两个正确示范，给 `X+bias` 例子加 seed 和 diff 验证证明 bias 在 batch/seq 上共享。
- 2026-04-27：第 0 阶段第 1 课自写练习推进。在 `01_vectors_and_axes_self_write.py` 新增 TODO-0（动手验证 `(3,)` / `(1,3)` / `(3,1)` 的差异，练习 reshape 与 `None`/`np.newaxis` 两种写法）；在 TODO-4 后加 4a/4b/4c（3D 张量上的 axis、`keepdims` 与多轴聚合 `axis=(0,1)`、`mean`/`max`/`argmax` 共用 axis 规则）；在 TODO-7 `can_broadcast` 用例里补 6 个 5 维 case（含 attention scores + mask 的真实形状）。
- 2026-05-01：推进第二阶段第 3 课论文精读，完善 [papers/notes/attention_is_all_you_need_reading_3.1_3.2.2.md](papers/notes/attention_is_all_you_need_reading_3.1_3.2.2.md)：补清 FFN/MLP/position-wise 的术语关系、两层 FFN 中 `W_1/b_1` + ReLU + `W_2/b_2` 的结构、残差连接与残差相加的区别、encoder/decoder `N=6` 是实验超参数和对称设计而非硬约束、cross-attention 的 Q/K/V 来源与 mask 差异、causal mask 中 `✓/✗` 与 `0/1` 约定的区别、multi-head 中 concat/拼接、head 随机初始化打破对称性，以及 `W^O` 是可学习输出投影并负责混合多个 head。
- 2026-05-01：新增 [papers/notes/gpt3_reading_2.1_model_and_architectures.md](papers/notes/gpt3_reading_2.1_model_and_architectures.md)，精读 GPT-3 `2.1 Model and Architectures`：梳理 GPT-3 与 GPT-2 架构关系、dense / locally banded sparse attention 交替、8 个模型尺寸与 Table 2.1 读表注意、`d_model / d_ff / d_head` 的含义、Scaling Law 中验证 loss 与"暴力出奇迹"的关系，以及学习率、batch size、300B tokens 和 Chinchilla 修正口径。
- 2026-05-01：补充 Scaling Law 与 Sparse Transformer 延伸阅读资料。在 `papers/scaling-laws/` 下载 Kaplan et al. *Scaling Laws for Neural Language Models* 与 Chinchilla *Training Compute-Optimal Large Language Models*；在 `papers/efficient-transformers/` 下载 Child et al. *Generating Long Sequences with Sparse Transformers*；同步更新 [papers/README.md](papers/README.md) 和 [phase2-transformer/README.md](phase2-transformer/README.md)，把这些资料加入后续学习计划。
- 2026-05-01：新增 [papers/notes/bert_reading_model_architecture.md](papers/notes/bert_reading_model_architecture.md)，精读 BERT `§3` 开篇、`Model Architecture` 与 `Input/Output Representations` 架构衔接段：补清预训练/微调范式、MLM 与 GPT left-to-right LM 的差异、`L/H/A/4H` 记号、`BERT_BASE / BERT_LARGE` 尺寸、双向 self-attention 与 causal mask 的区别、WordPiece / `[CLS]` / `[SEP]` / segment embedding，以及与 `03_multi_head_attention.py` 五个 Part 的对照。
- 2026-05-01：补齐第二阶段第 3 课自写练习 [phase2-transformer/03_multi_head_attention_self_write.py](phase2-transformer/03_multi_head_attention_self_write.py)：按 9 个 TODO 手写数值稳定 softmax、单头 attention、`split_heads` / `merge_heads`、完整 `multi_head_attention`、所有 head 共享 causal mask、残差连接、LayerNorm、Post-Norm / Pre-Norm，并通过内置校验。
- 2026-05-02：完成第二阶段第 3 课（多头注意力 / 残差连接 / LayerNorm / Pre-Norm vs Post-Norm）。跑通并复盘 [phase2-transformer/03_multi_head_attention.py](phase2-transformer/03_multi_head_attention.py)：补清 `reshape` + `transpose(1, 0, 2)` 的维度重排、每个 head 独立 attention 后先 `append` 收集再 `concatenate(axis=-1)` 拼回 `d_model`、LayerNorm 中 `var` 按每个 token 的特征维计算，以及 `eps` 防止分母为 0 或过小。下一步从第 4 课 [phase2-transformer/04_transformer_block.py](phase2-transformer/04_transformer_block.py) 开始，把 Attention、残差、LayerNorm 和 FFN 组合成完整 Transformer Block。
- 2026-05-25：在第二阶段新增**第 6 课「Scaling Law 与 Compute-Optimal 训练」**，补全项目里一直缺的 Scaling Law 章节。新增/修改：（1）[phase2-transformer/06_scaling_laws.py](phase2-transformer/06_scaling_laws.py)：Part 1 Kaplan 三条幂律 `L(N)/L(D)/L(C)`、Part 2 log-log 直线可视化、Part 3 `C ≈ 6ND` 与 `compute_optimal_chinchilla(C)`、Part 4 GPT-3/Chinchilla/Llama 3/Qwen2.5/DeepSeek-V3 在 N-D 平面对比、Part 5 三个手算复盘题；（2）[papers/notes/scaling_laws_kaplan_2020.md](papers/notes/scaling_laws_kaplan_2020.md)：Kaplan 2020 精读，讲清三条幂律 / `N` 为什么不含 embedding / loss 为什么用 nats / Kaplan 的 compute-optimal 结论（`N ↑ C^0.73`、`D ↑ C^0.27`）/ critical batch size；（3）[papers/notes/chinchilla_compute_optimal_2022.md](papers/notes/chinchilla_compute_optimal_2022.md)：Chinchilla 2022 精读，讲清三种 approach（固定 N 扫 D / IsoFLOP / 参数化拟合）如何一致指向 `N:D ≈ 1:20`、`C ≈ 6ND` 工程估算、Chinchilla vs Gopher 实战对比、Kaplan 错在哪（LR schedule + 固定步数），并加「Chinchilla 之后」一节覆盖 Llama 3 over-training（推理成本视角）、DeepSeek-V3 / Qwen2.5 实际选择、测试时 compute 第四变量；（4）同步更新 [phase2-transformer/README.md](phase2-transformer/README.md)（第 6 课大纲：视频 / 论文 / 代码 / 手算题 / 5 点关键领悟）、[papers/README.md](papers/README.md)（两篇 PDF 从「未读」升级为第 6 课配套）、根 [README.md](README.md) 课表与论文清单。脚本跑通，输出 `scaling_laws_part2.png`、`scaling_laws_part4.png`。
- 2026-05-25：新增**第五阶段「Agent 架构与 Agent 基础设施」**（分支 `phase5-agent-architecture`），面向 Agent infra 方向打基础。定位「应用与 infra 均衡」+「课程地图 + 动手代码」，共 10 课：1 Agent Loop / 2 Tool Calling / 3 规划与反思 / 4 记忆 / 5 RAG 与上下文工程 / 6 Agent Runtime 与状态 / 7 工具沙箱与安全 / 8 并发调度与缓存复用 / 9 可观测性评测成本 / 10 Multi-Agent 编排与框架对比。新增：[phase5-agent-architecture/README.md](phase5-agent-architecture/README.md)（10 课大纲 + 学习节奏 + 必读材料 + 4 个实践项目 + 应用↔infra 对照表）、三个零依赖可运行脚本 `01_minimal_agent_loop.py`（最小 agent loop + 终止条件）、`02_tool_calling.py`（tool 注册表 + dispatcher + 错误回灌）、`03_agent_runtime.py`（run/step + checkpoint/resume + trace 成本报告，含「模拟崩溃→断点续跑」演示）。三个脚本均用规则驱动 `FakeLLM`，无需 GPU/网络/API key 即可跑通骨架。同步更新根 [README.md](README.md) 结构树、路线图与课程总览。
- 2026-05-30：学完第二阶段第 4 课（Transformer Block）主课。跑通并逐段读懂 [phase2-transformer/04_transformer_block.py](phase2-transformer/04_transformer_block.py)：FFN 两层 MLP（升维 4 倍 + GELU + 降维）与"存储知识"的直觉、`W_Q` 为何是 `(d_model, d_model)`（打包所有 head 一起算再 reshape 切分）、`reshape` + `transpose(1,0,2)` 的维度重排、`K.transpose(0,2,1)` + 批量矩阵乘法（保留 head 维当 batch、省 for 循环）、多头输出 concat 回 `d_model` 再过 `W_O`、残差连接（信息保底 + 梯度高速公路）、Pre-Norm Block 数据流、堆叠 6 层数值稳定（残差 + LayerNorm 的功劳）、Dropout（inverted dropout：训练丢弃并缩放 `1/(1-rate)`、推理原样返回）。给主课补充了大量"为什么"注释、修正了 Part 编号错位、新增 `relu_vs_gelu.png` 对比图（函数值 + 梯度）讲清为何用 GELU。把本课配套自写练习 [phase2-transformer/04_transformer_block_self_write.py](phase2-transformer/04_transformer_block_self_write.py) 合并为一份 9 个 TODO 的完整版（softmax → LayerNorm → GELU → FFN → multi_head_attention → Pre-Norm Block → Post-Norm Block → 堆叠 N 层 → inverted dropout，含内置 `require_*` 校验、参数量统计与真实模型规模对比），并在 docstring 补了 Pre-Norm 数据流图与核心公式。
- 2026-05-30：完成第二阶段第 4 课自写练习 [phase2-transformer/04_transformer_block_self_write.py](phase2-transformer/04_transformer_block_self_write.py)，9 个 TODO 全部手写并通过内置校验（数值稳定 softmax → LayerNorm → GELU → 两层 FFN → multi_head_attention → Pre-Norm Block → Post-Norm Block → 堆叠 6 层验证数值稳定 → inverted dropout）。本轮关键收获：（1）LayerNorm 第 211 行「减均值除标准差」与下游 `mean≈0 / std≈1` 校验的对应关系，以及 γ/β 缺省时才严格成立；（2）多头注意力踩中两个 numpy 转置坑——reshape 后必须再 `transpose` 把 `num_heads` 提到最前、`K.T` 会反转所有轴需改成 `K.transpose(0,2,1)` 只换后两维（批量矩阵乘法保留 head 维当 batch）；（3）`scores = Q·Kᵀ/√d_k` 的 Query-Key 点积语义与 √d_k 缩放防 softmax 过尖；（4）inverted dropout 训练时 `x*mask/(1-rate)` 用 `1/(1-rate)` 补偿期望、推理直接原样返回。校验输出含参数量统计（单 Block 3,152，FFN 占约 68%）与 GPT-2/GPT-3/LLaMA-2 真实规模对比。下一步进入第 5 课，把 Token Embedding + Position Encoding + N 层 Block + LM Head 拼成迷你 GPT。
- 2026-06-06：开始第二阶段第 5 课（从零搭建 GPT），第二阶段首次进入 PyTorch 环境。已完成：（1）安装 PyTorch（`torch 2.12.0+cpu`，Python 3.14）；（2）跑通 [phase2-transformer/05_gpt_from_scratch.py](phase2-transformer/05_gpt_from_scratch.py)，观察到 220,544 参数的迷你 GPT 训练 loss 从 3.46（≈`ln(31)`，随机瞎猜基线）一路降到 0.31，并生成莎士比亚风格文本，验证了 Temperature 0.5/1.0/1.5 的确定性↔随机性差异；（3）给该文件入口补 `sys.stdout.reconfigure(encoding="utf-8")` 修复 Windows 中文乱码（已提交并推送，commit `f37be02`）；（4）重读了《Attention Is All You Need》论文。**待完成（明天继续）**：① 在本地自己亲手跑一遍第 5 课看效果；② 精读 Part 2 的 `CausalSelfAttention`（把论文公式 `softmax(QKᵀ/√dₖ)V`、`√dₖ` 缩放、masked attention、多头 concat + `W^O` 与代码第 96～118 行逐行对应）；③ 继续读 `TransformerBlock`（残差 + Pre-Norm，146～149 行）与 Part 3 完整 `GPT` 类。
- 2026-06-10：完善第 5 课学习设施（AI 协助，练习 TODO 全部留白待自己填）。（1）新增自写练习 [phase2-transformer/05_gpt_from_scratch_self_write.py](phase2-transformer/05_gpt_from_scratch_self_write.py)：10 个 TODO（因果掩码 → QKV 分头 → 注意力核心计算 → FFN → Pre-Norm Block → GPT.forward → get_batch → Top-K 过滤 → generate →（进阶可跳过）Top-P），各类 `__init__` 已给出、只写 forward；校验含「改未来 token 不影响过去 logits」的因果性测试和「top_k=1 等价贪心解码」检查，全部通过后脚本会用自己写的模型真训 300 步并生成文本。（2）修复主课 `05_gpt_from_scratch.py`：`torch.arange` 补 `device=idx.device`（GPU 兼容）、区分 Part 3 演示配置与 Part 4 实际训练模型的参数量打印、补 weight tying 与 GPT-2 残差初始化的「为什么」注释。（3）补齐第 2/3/4 课重置脚本并新增 `reset_exercises_05.py`（已在临时副本上验证幂等、重置后可运行、05 重置结果与原始留白版逐字节一致）。（4）给第 2/3/4 课自写练习补 `sys.stderr` UTF-8 reconfigure，修复 `ValidationError` 报错信息在 PowerShell 下乱码的问题。（5）同步 phase2 README 与根 README 的进度标记、课程表与文件清单。下一步：按第 5 课节奏完成精读后亲手填 10 个 TODO。
