# 我的学习进度

> 记录个人推进轨迹、阶段状态与关键领悟。根目录 `README.md` 只保留项目导览和当前状态摘要。

## 当前状态

- 第 0 阶段：第 1 课进行中
- 第一阶段：三课已完成；新增第 4 课「优化器」（SGD/Momentum/RMSprop/Adam/AdamW + Adafactor/显存代价），三件套已搭好、6 个 TODO 待亲手填
- 第二阶段：第 5 课（从零搭建 GPT，首次进入 PyTorch）已完成；第 6 课（Scaling Law）已完成——按「理解类」定位（无 self_write 练习）跑通主课、看懂两张图、记住核心 takeaway
- PyTorch 专项（phase2→phase3 桥梁课，8 课）：三件套全部搭好、8 课主课均跑通、8 个 reset 一致、留白文件可编译；TODO 全部留白待亲手填（详见 `pytorch-essentials/`）
- 第三阶段：**第 1 课 LoRA self_write 已全部完成（6/6 TODO）**；**第 2 课量化 self_write 已全部完成（6/6 TODO）**——主课 Part 1-7 已精读。**第 3 课 RLHF 收尾中**：Part 1–5 已精读（Part 3 弄清 loss↔optimizer 靠同一批 Parameter 串联、Part 4 `compute_ppo_loss` 的 ratio/`exp`/`clamp` 圆心为 1、Part 5 DPO 两个减法与 sum/mean 不对称），**self_write TODO-1 `dpo_loss` 已完成（1/1，4 个校验全过）**。关课还差两件：画一张 SFT→RM→PPO 数据流图 + 读 DPO 论文 §3–4。之后再按 [`alignment-reading-map.md`](papers/alignment-reading-map.md) 走 Session A（Christiano → Ziegler → Stiennon → InstructGPT）与 Session B（FLAN / T0 对照 instruction tuning）；Session C 风险材料不阻塞本课关闭。**第 4 课（推理优化）已开学**：Part 1–2 精读完（KV Cache 只缓存 K/V 的不对称性、拼接=表尾追加行、对拍 3e-7 是浮点结合律而非 bug、`W_o` 是出口自由度而非头间通道 + OV/QK 电路视角、Pre-LN 里 `h` 与残差旁路的分工、`view`/`transpose`/`contiguous` 的 stride 内幕、offset causal mask 与 `past_len` 形状反推的边界、`q_len==1` 退化成 GEMV 导致 decode 内存带宽瓶颈）；本课无 self_write（按 phase3 分层属 ⚡跑通档），Part 3 起待读
- **📚 论文 backlog（just-in-time，别先囤读，焦虑无意义）**：KV Cache 4 篇（`papers/kv-cache/`，phase3 第 4 课时读）、ZeRO（`papers/distributed-training/`，phase3 第 5 课时读）、RAG 三件套（`papers/retrieval-augmented/`，到 phase5 RAG 课再读）、More Is Different / 大批量训练（scaling 延伸，随缘）。这些都是「资料已就位、待精读」，不构成进度欠债
- 第四阶段（DeepSeek 与推理优化）：课程已搭建，待学习
- 第五阶段（Agent 架构与 Agent 基础设施）：课程已搭建，面向 Agent infra 方向，待学习

## 进展记录

- 2026-08-01：**第三阶段第 4 课（推理优化）开学——精读 Part 1–2（KV Cache 原理 + `CachedTransformerBlock`），本轮只读不改代码**。围绕 [`04_inference_optimization.py`](phase3-training/04_inference_optimization.py) 前 220 行逐行追问，收获按「缓存 / 多头 / 掩码 / 张量内存」四组归拢。（1）**缓存的不对称性来源**：把单个位置的输出写成 `out_i = Σ_{j≤i} softmax(Q_i·K_j/√d_k)·V_j`，式子里 Q 只出现下标 `i`（当步提问、用完即弃），K/V 遍历 `j≤i`（被后续每一步反复查询）——所以缓存 K/V 划算、缓存 Q 无意义。K/V 可复用的根据是 `K_j=W_k x_j` 只依赖 `x_j` 自己，加上因果掩码保证后来的 token 不改变前面 token 的表示，算一次永远有效。推论：**decode 步天然不需要 causal mask**（缓存里只有「过去+自己」）。（2）**拼接就是「往表尾追加一行」**：`K` 去掉 batch 维就是一张「一行一个 token」的表，`torch.cat(dim=1)`（多头是 `(B,H,T,hd)` 故 `dim=2`）在尾部加一行；拼完**行号即位置**，所以顺序必须 `[K_cached, K_new]`，反了不报错但历史乱序、下游掩码与位置编码全错。代价是 `cat` 非原地，每步复制整个旧缓存，decode T 步累计 `O(T²)` 拷贝；生产改预分配 `K_buf[:, t] = K_new` 原地写，再进一步是 PagedAttention 分页管理。（3）**对拍差 `3e-7` 不是 bug，是浮点加法不满足结合律**：无缓存走 `(10,64)@(64,10)` 的 GEMM、有缓存走 `(1,64)@(64,t+1)` 的 GEMV，BLAS 的分块/SIMD/FMA 路径不同 → 累加顺序不同 → 末位不同；float32 机器精度 `2^-23≈1.19e-7`、输出量级个位数，`3e-7` 正是最后一两个二进制位。所以 `assert_close(atol=1e-6, rtol=1e-5)` 是刻意留的余量——真写错（顺序反 / 漏拼接）误差是 0.1 量级一定抓得住，而写 `==` 反倒会让正确实现挂掉。结论：**KV Cache 数学上精确、数值上近似**；实际后果是 top-1/top-2 的 logits 接近时末位误差能翻转 argmax，同一 prompt 同样 greedy 也可能分叉。追问「换 int 是否就没误差」的答案是**因小失大**：整数加乘在不溢出时精确且满足结合律（int8×int8 用 int32 累加器完全够），但 softmax 需要 `exp` 和除法、整数域没有对应物（INT8 推理实际是「矩阵乘走整数、反量化回 float 做 softmax」）；更关键是 float32 相对误差 `1e-7`，而 int8 量化相对误差 `1e-2`，**大五个数量级**。**整数换来的是确定性（determinism）不是精度（accuracy）**，真想可复现该用 `torch.use_deterministic_algorithms(True)` + 锁 cuBLAS 配置 + 固定 batch size。（4）**`W_o` 不是「头间交流的通道」，而是「出口的自由度」**（纠了自己一个错觉）：同层四个头前向完全独立，`W_o` 是线性的、只能加权求和、没有乘性交互，谈不上互相影响。把它横切成 4 块 `16×64`，`Concat(h)·W^O = Σ h_i·W_o^i`——每个头用自己那块把结果**写回残差流**；删掉 `W_o` 的后果不是「少一次混合」，而是头 `i` 被焊死只能写残差流第 `[16i,16i+16)` 维。再往下：`W_v^i` 与 `W_o^i` 从不单独存在，复合成一个 `64×64` 但 **rank≤16** 的矩阵（Anthropic *A Mathematical Framework for Transformer Circuits* 叫 **OV 电路**），`W_q^i/W_k^i` 同理复合成 **QK 电路**。于是**多头的本质 = 把一个满秩变换拆成 4 个秩≤16 的低秩变换 + 4 套独立的注意力路由**，参数量不变（都是 `4d²`），用「每条通路表达力变弱」换「同时维持多套关注模式」——之所以划算，是因为指代/句法/词性这些依赖需要并存追踪，但单条关系本身并不需要满秩去刻画。真正的融合在紧接着的 FFN 非线性和下一层注意力。补一层：**前向独立、反向耦合**——四个头没有各自的 loss，梯度经 `W_o` 分流回去，两个头学成一样就贡献冗余、梯度推动它们分化，所以「这个头管指代那个头管句法」是优化自发的分工；`W_o` 因此兼任 **credit assignment** 通道，某个头的 `W_o^i` 被学到接近 0 就既不影响输出也收不到梯度而「死掉」，这正是「很多 head 可以直接剪掉不掉点」的机制。`bias=False` 的理由：Pre-LN 下输出加进残差后，下一层开头的 LayerNorm 会减均值，常数偏置立刻被消掉，学它没意义。（5）**`h = self.ln1(x)` 必须另起变量，不能写成 `x = self.ln1(x)`**：Pre-LN 的要点是「归一化只作用于进入子层的那份数据，残差旁路始终连未经改动的 `x`」，覆盖掉 `x` 会切断恒等映射路径、梯度每过一层被缩放一次，几十层叠起来退回梯度消失。`h` 被 `W_q/W_k/W_v` 用三次才值得起名，FFN 那侧的 `ln2(x)` 只用一次就内联了。（6）**`view` 与 `transpose` 的顺序不能换**：`W_q(h)` 内存按 `(B,T,C)` 连续、同一 token 的 64 个数相邻，所以必须先 `.view(B,T,H,hd)`「在最后一维上切」再 `.transpose(1,2)` 把 head 提前；直接 `.view(B,H,T,hd)` 是按内存顺序跨 token 乱切，**形状合法、结果全错、还不报错**。`transpose` 的目的是让 `(T,head_dim)` 落到最后两维——PyTorch 的 `@` 只对最后两维做矩阵乘、前面维度全当 batch，于是 `B×H` 个头一次并行；不换位则退化成「对每个 token 各做一次 4×16 乘法」，token 之间根本没交互、注意力的核心直接没了。（7）**`contiguous` 的 stride 视角**（跑了个 `(2,3)` 小例子验证）：张量 = 一块平铺内存 + `(shape, stride)` 说明书；`x.t()` 后 `data_ptr` **完全不变**、只把 stride 从 `(3,1)` 换成 `(1,3)`，所以 transpose 近乎零成本；「连续」的定义是「按行优先遍历的顺序 == 内存顺序」，转置后逻辑序 `[0,3,1,4,2,5]` ≠ 内存序 `[0,1,2,3,4,5]` 故不连续；`contiguous()` 真按逻辑序写一块新内存（`ptr` 变了）。关键纠偏：**不是「view 挑剔所以多做一步」，而是这次重排本来就必须发生**——换 `.reshape()` 照样拷贝，只是不报错、悄悄替你做；view 的严格是诚实。真要省掉得让 attention kernel 直接按 token 优先布局写出结果（FlashAttention 的 BSHD），省的是一整趟对 `B×T×C` 的读写。（8）**offset causal mask**：`q_len ≠ seq_len` 时 `tril` 造不出方阵，改用位置比较 `arange(past_len, seq_len)[:,None] <= arange(seq_len)[None,:]`。这张 `(q_len, seq_len)` 掩码就是完整 `L×L` 掩码的**最后 q_len 行**——左边 `past_len` 列全通、右边 `q_len` 列才是三角，整体是平行四边形。位置比较是通用惯用法（加一个条件就能表达滑动窗口，`tril` 做不到），`unsqueeze` 的作用是给广播留插槽：`(q,1)` vs `(1,L)` 才能广播成外积式的 `(q,L)`，两边都横着只能一对一比较；掩码补成 `(1,1,q,L)` 靠广播共享，不真复制 `B×H` 份。**真正的捷径是 `past_len = seq_len - q_len`**（起初觉得整段「投机取巧」，其实错怪了掩码写法）：它从形状反推位置，隐含三个假设——缓存是连续完整的历史（滑动窗口 / H2O 淘汰后失效）、缓存无 padding、batch 内长度一致；生产因此显式传 `cache_position` + `attention_mask`。顺带一个真实坑：`q_len≠kv_len` 时「因果」有左上 / 右下两种对齐约定，KV Cache decode 要的是右下对齐，PyTorch SDPA 的 `is_causal` 与 FlashAttention 约定并不一致，换现成 kernel 必须查文档。**为什么不造完整 `L×L`**：最根本是 `scores` 物理上只有 `q_len` 行（老 token 的 Q 早就丢了，那些行不是「算了不用」而是「不存在」）；其次每步重造累计 `O(L³)`；再者 32K 上下文的 bool 掩码就是 1GB 且 99.99% 用不到。GPT-2 式 `register_buffer` 预造 `max_len²` 再切 `[key_len-query_len:key_len, :key_len]` 是合法变体（切的正是同一个偏移矩形），但受 `max_len` 硬限制——128K 要 16GB，这就是长上下文模型全部放弃预造掩码的原因。（9）**`q_len == 1` 标记的是最难优化的阶段**：掩码此时必然全 True（唯一 query 在序列末尾），跳过是精确而非近似。更要紧的是形状退化——`Q@K.T` 从 GEMM 掉成 GEMV，而且不止注意力，`W_q/k/v/o` 与 FFN 的全部线性层输入都只有 1 个 token；GEMV 算术强度极低，为一个 token 前向要把**整个模型权重**从显存搬一遍而每个权重只用一次，于是 **decode 是内存带宽瓶颈、算力大量闲置**（MFU 常在个位数），与 prefill 的 compute-bound 完全相反。这一条把后半课的动机全串起来了：continuous batching = 权重反正要读一遍，摊薄给更多 token；投机解码 = 小模型猜 k 个、大模型一次验证 k 个（此时 `q_len=k>1`，偏移掩码分支才真正用上），本质是拿闲置算力换带宽；量化 = 直接砍掉权重读取量。验证：本轮只读不改主课代码（此前已 exit 0），期间跑了两个一次性小实验确认理解（广播比较的真实输出、`stride`/`data_ptr` 的变化）。**下一步**：精读 Part 3 起（采样策略、投机解码），并按 backlog 读 KV Cache 4 篇（[`papers/kv-cache/`](papers/kv-cache/)：MQA → GQA → PagedAttention → FlashAttention）——本课正是它们 just-in-time 的时机。**（提醒：第 3 课 RLHF 的关课两件事——画 SFT→RM→PPO 数据流图 + 读 DPO §3–4——仍挂着，按效率四规则第 1 条「先关线再开线」应尽快收掉。）**
- 2026-07-30：**完成第 3 课 RLHF 唯一的手搓项——self_write TODO-1 `dpo_loss`（1/1），4 个校验全过**（策略=参考 → loss=0.6931≈log2；chosen 占优 → 0.5981<log2 且 acc=100%；方向反 → 0.7981>log2 且 acc=0%；β 0.1→1.0 → loss 0.5981→0.1269）。**学习节奏上的一个判断**：InstructGPT 引用链读到发腻，回头查 `phase3-training/README.md` 才确认第 3 课是 📖 精读档、done 标准只有「画数据流图 + 亲手推 DPO loss」，那批对齐/风险论文本就标着「不阻塞关课」——把配菜当主菜吃才是腻的原因，于是先回键盘填 TODO。呼应效率四规则第 2 条「设施再完美，不填 TODO = 没学」。**三点关键领悟（均已写进主课 [`03_rlhf.py`](phase3-training/03_rlhf.py) `dpo_loss` 注释）**：（1）**公式里两个减法语义完全不同，别混成一件事**。减法一 `logπ(y) - logπ_ref(y)`（chosen/rejected 各自减）是**消掉起跑线**：绝对 logprob 里混着「句子固有难度」（长度、词频，跟好坏无关）和「模型多想说它」（唯一关心的），序列 logprob 是逐 token 累加负数，长回答天然更负，不减 ref 直接比大小会误判成「模型偏好那个敷衍的短答」；π 与 π_ref 面对同一句子、同样难度，一减就抵消（类比：比两个学生谁进步大要看「期末 − 期中」而非期末绝对分）。减法二「chosen 位移 − rejected 位移」是**构造零和拉扯**：① 人类只给了「A 比 B 好」一个 bit、没有绝对分数，能建模的只有差值（Bradley-Terry）；② 若只抬 chosen，模型可以「把所有回答一起抬高」作弊，相减后整体同抬 margin 不变、loss 不动，只能抬 chosen 或压 rejected——这就是对比学习的骨架；③ 也正是这一减让配分函数 `Z(x)` 抵消（只依赖共享的 prompt）。（2）**token 维必须 `sum`、batch 维用 `mean`，这个不对称是刻意的**。token 维 sum 是概率链式法则 `log P(y|x)=Σ_t log p(y_t|·)`，取平均就不再是「序列的对数概率」、闭式解与 `Z(x)` 抵消整套推导失效；batch 维 mean 是工程选择——`backward` 只能对标量调用（`(batch,)` 会报 `grad can be implicitly created only for scalar outputs`），且用 mean 而非 sum 让梯度量级与 batch size 解耦（sum 等于偷偷把 lr 乘 batch_size），同 `nn.CrossEntropyLoss` 默认 `reduction='mean'`；归约的必要性来自「它是张量」而非「有多个元素」，batch=1 时 `(1,)` 一样要归约。**sum 的代价 = length bias**：长回答 logprob 更负、梯度尺度更大，减 ref 只消起点差异消不掉它，所以 DPO 爱生成又长又啰嗦的回答，SimPO 等改用除以长度的 length-normalized logprob 就是故意打破这个 sum——与 off-policy（`y_w/y_l` 非 `π_ref` 自采样时推导前提被破坏）并列为 DPO 两个公认局限。另外 `acc` 那行的 `.float().mean()` 只是统计「排对比例」当指标、不参与 backward，`.float()` 省不掉（bool 张量不能 mean）。（3）**`y_w`/`y_l` 从哪来**：真实 RLHF（InstructGPT 第二步）是 SFT 模型对同一 prompt 自采样 K 个回答（K=4~9，靠温度/nucleus 制造多样性），人类**只排序不打分**，一个 K 元排序展开成 `C(K,2)` 个 pairwise 对；InstructGPT 特意把同一 prompt 的所有对放进**同一个 batch**，否则每个回答会被反复训练 `K-1` 次而过拟合。省力点在于「让人写好答案很贵、让人从候选里挑很便宜」，正是 Christiano 2017 的原始洞察；现在也可用公开偏好集（HH-RLHF / UltraFeedback）或更强模型当裁判（RLAIF / Constitutional AI）。主课玩具版则把「人类偏好」定义成**有规律 > 随机**（`chosen=(prompt+1)%vocab_size`、`rejected` 纯随机）。验证：主课 `exit 0`、self_write 4 校验通过、无 lint。**下一步（关课还差两件）**：画一张 SFT→RM→PPO 数据流图 + 读 DPO 论文 §3–4 把符号映射回 `policy_chosen`/`ref_chosen` 四项。
- 2026-07-28：**把 InstructGPT 两段 Related Work 一并收束成可执行阅读地图（资料整理完成，论文尚未精读）**。没有把四十多条引用全部塞进 backlog，而是按当前 Phase 3 问题分层：（1）Session A 90 分钟补齐 RLHF 方法链——Christiano（轨迹偏好）→ **Ziegler 2019（首次系统迁移到四类自然语言任务，也展示标注捷径 / bug 会被策略利用）**→ Stiennon（高质量摘要 RLHF 与 reward over-optimization）→ InstructGPT（广泛书面指令）；随后立刻返回 DPO TODO-1 + 论文 §3–4。（2）Session B 45 分钟用 **FLAN + T0** 回答「多任务 instruction tuning 已能跨任务泛化，RLHF 额外解决什么」。（3）DPO 后用 Askell / Gabriel / Kenton / Weidinger 定点建立 HHH、价值目标与风险分类；Carlini / Xu / PALMS 分别作为隐私提取、detox 公平性副作用、小规模 values-targeted 微调案例。新增 11 篇完整 PDF，页码 / 问题 / 暂缓引用统一写入 [`papers/alignment-reading-map.md`](papers/alignment-reading-map.md)；导航、特定领域偏好学习、benchmark 与旧式生成控制方法明确 just-in-time，不构成当前欠债。并订正主课注释：HHH 的直接来源是 Askell et al. 2021，InstructGPT 沿用。
- 2026-07-27：**追溯 InstructGPT 的 RLHF 引用链并插入第 3 课学习计划（未开始精读）**。针对原文引用的 Christiano et al. (2017) 与 Stiennon et al. (2020)，结论不是再加两篇全文债：Christiano 是「从轨迹两两偏好学习奖励」的思想源头，只定点读 Figure 1 + §2.2.3（15 分钟）；**Stiennon《Learning to Summarize from Human Feedback》是当前最该补的论文**，因为它把方法系统落到 GPT 风格文本生成，直接给出「比较数据→RM→PPO」三段式、pairwise RM loss、`reward - β·KL(policy‖SFT)`，还用 Figure 5 展示 reward model 被过度优化后预测奖励继续涨、真实人类偏好却崩掉，只精读 §3.1 / Figure 2 / §3.4 / §4.3 Figure 5（30 分钟）；最后回看 InstructGPT Figure 2 + §3（15 分钟），看它怎样从单任务摘要扩展到广泛书面指令。PDF 已加入 [`papers/efficient-training/`](papers/efficient-training/)，读完这 60 分钟即返回原计划：填 DPO TODO-1 → DPO §3–4。
- 2026-07-22：**第 3 课 RLHF 复工温习（不填 TODO）**。搁置约两周后先唤回大图（SFT→RM→PPO；DPO 替代 RM+PPO），再精读主课两处并补注释：（1）**Part 3 奖励模型训练**：弄清 `loss.backward` / `optimizer.step` 并不互相「认识」，纽带是同一批 `nn.Parameter`——forward 把计算图绳子接到参数，算术续绳子到 loss，backward 写 `.grad`，optimizer 只更新登记过的参数。（2）**Part 4 `compute_ppo_loss`**：`ratio=exp(logπ-logπ_old)`（log 空间做比更稳）、clip 圆心 `1` 来自「π_new=π_old 时比值为 1」、`clamp` 锁相对偏离 ±ε、`min(surr1,surr2)` 取更保守目标。self_write TODO-1 仍留白，下次再填。
- 2026-07-08：**第 3 课 RLHF 精读 Part 4 PPO + Part 5 DPO，均落到代码/注释**。主课 [`03_rlhf.py`](phase3-training/03_rlhf.py) 两处改动：（1）**PPO 训练循环重写为真正的两阶段结构**——原来是「每步重新采样 + 只更新一次」，导致 `old_log_probs` 恒等于 `new_log_probs`、ratio≡1、clip 形同虚设（自己看代码时发现的）。改成「阶段 1 采样一次并冻结 old/rewards/ref → 阶段 2 在同一批数据上内层更新 `ppo_epochs=4` 次」，从第 2 轮内层更新起 new≠old、ratio 真正偏离 1，clip 才有意义；打印新增 `ratio[min/max]` 直观验证（实测约 0.96~1.01）。顺手修了一个 bug：`ref_log_probs` 被用到却漏定义（会 NameError），补回阶段 1；并去掉 no_grad 块内多余的 `.detach()` 统一风格。关键领悟：**PPO 有两个不同尺度的「参照物」——`old`（每 step 刷新的滚动起点，配 ratio+clip 管「单批别迈太大」）vs `ref`（全程冻结的 SFT 原点，配 KL 惩罚管「累积别偏太远」），缺一不可**（每批相对 old 只挪一小步，累加仍可能离 ref 越来越远）。这些都写进了循环注释。（2）**DPO 损失「为什么 work」的推导写进 `dpo_loss` 上方注释**：RLHF 目标 `max E[r]-β·KL` 有闭式最优解 `π*∝π_ref·exp(r/β)` → 反解出隐式奖励 `r=β·log(π/π_ref)+β·logZ`（甩掉奖励模型）→ 代入 Bradley-Terry 做差时配分函数 `Z(x)` 抵消（甩掉采样/RL）→ 只剩监督式二分类损失；梯度自带难度加权（排对的样本梯度≈0、排反的最大）。结论：DPO 不是 PPO 的近似，而是同一目标的精确等价重写。下一步：填 [`03_rlhf_self_write.py`](phase3-training/03_rlhf_self_write.py) TODO-1（DPO loss）→ 读 DPO 论文 §3–4。
- 2026-07-08：**第 3 课 RLHF 开学 + RL 对齐必读论文就位**。（1）下载 3 篇 RL 对齐论文到 [`papers/efficient-training/`](papers/efficient-training/)——**PPO**（Schulman 2017, arXiv:1707.06347，RLHF 默认优化算法）、**Deep RL from Human Preferences**（Christiano 2017, arXiv:1706.03741，奖励模型思想源头）、**Constitutional AI**（Anthropic 2022, arXiv:2212.08073，RLAIF）；DPO 与 GRPO 分别已在 `efficient-training/` 与 `deepseek/DeepSeekMath`，不重复下载。（2）[`papers/README.md`](papers/README.md) 新增「强化学习对齐（RLHF / PPO / DPO / GRPO / RLAIF）必读」专题，给出 PPO → Christiano → InstructGPT → DPO → Constitutional AI → GRPO 阅读主线；根 [`README.md`](README.md) 核心论文表同步补 5 行。（3）主课 [`03_rlhf.py`](phase3-training/03_rlhf.py) 补 InstructGPT 论文路径、HHH 标准出处、RewardModel 里 head 与 multi-head attention 的区分注释；[`phase3-training/README.md`](phase3-training/README.md) 补 InstructGPT/DPO 配套论文表与「代码先行、论文对照」的 DPO 学习顺序。下一步：跑通 Part 5 `dpo_loss` → 填 self_write TODO-1 → 读 DPO 论文 §3–4。**完成第三阶段第 2 课量化自写练习（6/6 TODO 全部通过）**。[`02_quantization_self_write.py`](phase3-training/02_quantization_self_write.py) 亲手实现并校验：（1）**TODO-1/2 对称量化**：`scale=x.abs().max()/qmax`（不能用 Python `max(tensor)`）、`clamp(round(x/scale))` + `.to(int8)`、还原 `q.float()*scale`；0 精确映射、误差 ≤ 一个 scale。（2）**TODO-3 非对称量化**：`zero_point=round(-x_min/scale)`（负号把浮点 0 平移到整数轴）、`q=round(x/scale)+zero_point` 转 uint8；全非负 ReLU 数据非对称误差 ≤ 对称。（3）**TODO-4 逐通道**：`weight.abs().max(dim=1,keepdim=True)[0]` 每行独立 scale，`squeeze()` 存 `(rows,)`、反量化 `unsqueeze(1)` 广播；实测逐通道误差 0.00975 < 逐张量 0.02774（2.8x）。（4）**TODO-5 STE**：`backward` 返回 `(grad_output, None)`，梯度绕过不可导的 round。（5）**TODO-6 字节估算**：`numel*num_bits/8`，LLaMA-70B FP32 260.8GB→INT8 65.2GB→INT4 32.6GB。踩坑：张量运算必须用 PyTorch API（`x.abs().max()` 而非 `max(abs(x))`）。下一步：第 3 课 RLHF 或回到主课 Part 6 不同位数对比。
- 2026-07-07：**第三阶段第 2 课量化推进——吃透 Part 5 完整模型量化与 Part 7 QAT/STE**。继续精读主课 [`02_quantization.py`](phase3-training/02_quantization.py) Part 5 + Part 7：（1）**Part 5 完整模型量化**：`QuantizedLinear` 构造时 `per_channel_quantize` 把 weight 压成 INT8 + 每行 scale，推理时 `dq_weight = q_weight.float() * scales.unsqueeze(1)` 临时反量化再 `F.linear`——典型 PTQ「存 INT8、算 FP32」；`quantize_model` 用 `named_children()` 遍历直接子模块，`isinstance(Linear)` 则 `setattr` 原地替换，否则递归，对 `SimpleModel` 三个 fc 全换；须先跑 FP32 输出再量化（`quantize_model` 原地改 model）。（2）**`register_buffer`**：挂「要保存/搬设备、但不训练」的张量——不进 `parameters()`、优化器碰不到，但 `state_dict()`/`to(device)` 会带上；`q_weight`/`scales`/`bias` 都是 PTQ 算好固定的，比 `nn.Parameter` 或裸属性 `self.xxx=` 语义正确。（3）压缩比 ~3.5x 而非 4x：只有 weight 1 字节/元素，bias 仍 FP32，每行还多一个 scale。（4）**Part 7 STE**：`round` 不可导会阻断梯度；`FakeQuantize.backward` 直接 `return grad_output`（直通估计器）——前向真量化模拟噪声，反向假装恒等映射，QAT 才能在「伪量化环境」里继续训练。下一步：填 self_write TODO-1~4，或继续 Part 6 不同位数对比。
- 2026-07-07：**第三阶段第 2 课量化推进——吃透非对称量化与逐通道量化**。继续精读主课 [`02_quantization.py`](phase3-training/02_quantization.py) Part 3-4：（1）**非对称量化**：整数轴是 `uint8` 的 `[0,255]`（全非负），而浮点可能有负数/起点不为 0，故引入 `zero_point = round(-x_min/scale)` 把浮点 0 搬到整数轴对应格子、令 `[x_min,x_max]` 平移铺满 `[0,qmax]`；对 ReLU 这种全非负输出，对称量化会浪费负半轴一半格点、非对称能全用上，故误差更小。（2）**担心「内层 `+zero_point` 会不会让 x_min 端变负」→ 不会**：`x_min` 处 `= round(x_min/scale)+round(-x_min/scale)`，两边同一个 `|a|`、round 就近取整对称，正负抵消恒为 0；即便浮点误差越界还有外层 `clamp(_,0,qmax)` 兜底。（3）**逐通道量化**：公式与对称量化相同，唯一区别是 scale 从全局标量换成「每行一个」——`abs_max=weight.abs().max(dim=1,keepdim=True)[0]`（`dim=1` 压掉列=每行一个、`keepdim` 保 `(N,1)` 供广播、`[0]` 取 values 丢 indices），靠 `(N,1)` 与 `(N,C)` 广播让每行用自己的尺子；反量化 `s.unsqueeze(1)` 恢复形状乘回（存扁平、用时恢复）。**为何逐行**：`nn.Linear` 权重是 `(out,in)`，逐行=逐输出通道，解决「全局 scale 被极端大行主导、小行被压成 0」的问题，主课实测精度提升数倍。以上心得（round 对称抵消、zero_point 含义、逐通道反量化广播）已写进主课注释。下一步：填 self_write [`02_quantization_self_write.py`](phase3-training/02_quantization_self_write.py) 的 TODO-1~4（对称/反量化/非对称/逐通道）。
- 2026-07-06：**开始第三阶段第 2 课（模型量化），吃透对称量化前半段**。跑通主课 [`02_quantization.py`](phase3-training/02_quantization.py) Part 1-2，逐点搞懂：（1）**FP32/FP16/BF16 对比**——三者都是浮点，FP16「范围小但精度保留较多」（5 指数 +10 尾数），BF16「范围大但精度丢较多」（8 指数 +7 尾数、与 FP32 同档范围），记成「FP16 用范围换精度、BF16 用精度换范围」，训练偏爱 BF16 是因为「先别溢出」比多几位小数更重要；INT8 是整数，不能直接 `.to(int8)`（无 scale、无还原路径、量级差异大），必须走量化。（2）**对称量化 `symmetric_quantize` 逐行**：`scale=max|x|/qmax`、`q=clamp(round(x/scale),qmin,qmax).to(int8)`、还原 `q*scale`。（3）**为何 `qmax=2^(n-1)-1` 用对称范围 [-127,127] 而非补码全范围 [-128,127]**——故意丢掉 -128 保证正负对称、0 精确映射到整数 0（4 位同理 [-7,7]，指数是 `n-1` 因为 1 位给符号）。（4）**round 与 to(int8) 分工缺一不可**：round=就近取整（误差压到最多半个 scale、仍是浮点只改「值」），to(int8) 只改类型/存储（4 字节→1 字节、省空间靠它）；只截断不 round 会朝 0 偏、误差更大。已把 round/to(int8) 这条心得写进主课注释。（5）显存动机口算：70B FP32 280GB→INT8 70GB(÷4)→INT4 35GB(÷8)。下一步：填 self_write [`02_quantization_self_write.py`](phase3-training/02_quantization_self_write.py) 的 TODO-1/2（对称量化+反量化），再推进非对称/逐通道量化。
- 2026-07-06：**完成第三阶段第 1 课 LoRA 自写练习（6/6 TODO 全部通过）**。[`01_lora_self_write.py`](phase3-training/01_lora_self_write.py) 亲手实现并校验：`LoRALinear` 初始化（B=0 恒等旁路、scaling=α/r）、`forward`（`W·x + (x@A@B)·scaling`）、`apply_lora`（全冻再解冻 LoRA）、`merge_lora`（`delta_w.T` 写进权重 + `lora_B.zero_()`）、`lora_param_count`（`in*r + r*out`，d=4096,r=8 压缩 256x）、TODO-6 整模型 merge 前后 `no_grad` 推理一致。关键领悟：（1）训练保留旁路、merge 后旁路失效，数学等价 `W·x+(BA)·x → (W+BA)·x`；（2）合并后必须清零 `lora_B`，否则 forward 双算 ΔW；（3）`delta_w (in,out)` 与 `nn.Linear.weight (out,in)` 差转置；（4）TODO-6 的意义是端到端验证「敢部署」而不只是单层公式对。脚本收尾 LoRA 微调 loss 2.916→0.034。下一步：读主课 `01_lora.py`、试 rank=1/4/8/16，进入第 2 课量化。
- 2026-06-25：**完成第二阶段第 6 课（Scaling Law 与 Compute-Optimal 训练）**。本课按「理解类」定位收尾（不像 attention/transformer block 那样有 `self_write` 手写练习，本就是把两篇论文结论翻译成可口算公式），方式是跑通 [phase2-transformer/06_scaling_laws.py](phase2-transformer/06_scaling_laws.py)、看懂 `scaling_laws_part2.png`（三条幂律在 log-log 上是直线）与 `scaling_laws_part4.png`（工业模型在 N-D 平面的分布），记住核心 takeaway。关键收获：（1）三条幂律 `L(N)/L(D)/L(C)` 在 log-log 图上是直线 = 可外推，斜率即指数 α（D 的 α=0.095 比 N 的 0.076 更陡、数据更「值」）；（2）`C ≈ 6ND` 口算神器（前向 2N + 反向 4N），给 N 和 D 秒估训练算力，是本课唯一需要「会用」的公式；（3）Chinchilla 修正 Kaplan——算力固定时 N 和 D 该按 ~1:20 同步扩，Chinchilla 70B 用同算力打败欠训的 Gopher 280B；（4）训练最优 ≠ 推理最优——工业界（Llama 3 / Qwen2.5 / DeepSeek-V3）故意 over-train 到 1:200~1:2000，因为推理要跑无数次、模型练小练久长期更省（inference-aware scaling），看 Part 3 演示 1 的 D/N 列最直观：GPT-3=2 / Chinchilla=20 / Llama 3-8B=1875；（5）以上都是「量变」，做大的终极动机是能力涌现（emergence）这种「质变」。下一步：进入第三阶段（训练 / 微调 / 量化 / 推理优化）。
- 2026-06-25：**复盘学习节奏，给 phase3 换挡（效率/方法专项）**。诊断：最近两周的进展几乎全是「囤资料 / 搭脚手架」（RAG 三件套、ZeRO、More Is Different、KV Cache 4 篇、大批量训练论文、PyTorch 8 课、优化器课），而「亲手消化」严重滞后——待精读论文 + 待填 TODO 持续累积从未清零，同时挂着 phase0/phase1-4/phase2-6/PyTorch-8 课多条未关闭的线。根因：phase0-2「全程 NumPy 从零手搓 + 三件套」的打法在基本功阶段是对的，但带进**工程/应用性质的 phase3** 会翻车（RLHF 从零搓 PPO 能吃掉一周）。对策：（1）把 [phase3-training/README.md](phase3-training/README.md) 课程结构重写为**「分层 + 时间盒」**——三档学法 🔨手搓（只 LoRA + 量化核心）/ ⚡跑通（训练流程、推理）/ 📖精读看图（RLHF 防陷阱、分布式），每课带 done 标准 + 建议天数，合计约 2-2.5 周（对比老节奏 1.5 个月）；（2）新增「学习方式（效率四规则）」：先关线再开线、区分建设vs学习（脚手架交给 AI、人只填 TODO+跑实验）、按档位投力气、每课守时间盒；（3）当前状态里把 KV Cache / ZeRO / RAG / scaling 延伸论文明确降级为 **just-in-time backlog**（学到哪读哪篇，不构成进度欠债）。结论一句话：phase3 真正值得亲手写的只有 LoRA 和量化核心，其余「读懂+跑通+能改参数」即可。**同日动手精简 phase3 内容**：删除 [phase3-training/01_training_pipeline.py](phase3-training/) 整课（DataLoader/五步曲/AMP/梯度累积/LR调度/checkpoint/整合 8 个 Part 全部被 PyTorch 专项第 4/5/6/8 课覆盖，纯重复）；删前把其 Part 5 里那段有价值的 critical batch size / B_crit 交叉引用（McCandlish 2018）搬进 [pytorch-essentials/06_training_loop.py](pytorch-essentials/06_training_loop.py) Part 7（梯度累积），不丢内容；同步改掉所有指向旧文件的活引用（根 README 结构树+课表、phase1 第4课优化器注释、McCandlish 笔记的配套课程指向）。**随后把剩余 4 课文件重命名为从 01 开始**：`02_lora→01_lora`、`03_quantization→02_quantization`、`04_rlhf→03_rlhf`、`05_inference_optimization→04_inference_optimization`（`kv_cache_numpy_demo.py` 无编号保留），各文件内课号标题/运行命令/结尾同步下移一位，分布式专题从「第 6 课」改为「第 5 课」；并更新所有活引用（根 README、phase3 README、papers/frontier-ai、phase4 README、kv demo）。历史日志条目里的旧文件名作为快照保留不改。phase3 现为 4+1 课（01-04 + 分布式待补）、约 1.5-2 周。**最后通读 4 节课内容确认无需再删改**（LoRA / 量化质量扎实，RLHF 的 PPO 已是简化版、DPO loss 单独成函数，推理课 top-k/top-p 在 phase2 第5课已手写过、KV Cache 有 numpy demo），并在 [phase3-training/README.md](phase3-training/README.md) 补「整体学习规划」：① self_write 决策表——只第 1 课 LoRA（~5 TODO）+ 第 2 课量化（~6 TODO）配留白，第 3 课 RLHF 只 `dpo_loss` 一个可选 TODO（PPO 不手搓），第 4/5 课不配 self_write（重复 / 概念课）；② 8-9 天推进节奏表；③ 每课「关闭」标准（跑通+填完 TODO/画图推导+写进展）。self_write 文件按 just-in-time 建（开始哪课才搭那课留白+reset，不提前全搭）。**随后用户决定「全部开干」并放弃 reset 脚本，phase3 改为「两件套」（主课 + self_write，无 reset）**，一次建好 3 个留白：[01_lora_self_write.py](phase3-training/01_lora_self_write.py)（5 TODO：LoRALinear 初始化/forward 旁路/apply_lora 冻结/merge_lora 转置对齐/参数量手算）、[02_quantization_self_write.py](phase3-training/02_quantization_self_write.py)（6 TODO：对称/非对称/逐通道量化 + STE 伪量化 backward + 字节估算）、[03_rlhf_self_write.py](phase3-training/03_rlhf_self_write.py)（仅 `dpo_loss` 1 TODO，自带 4 个性质校验：策略=参考→log2、偏好正确→loss↓acc=1、方向反→loss↑acc=0、β↑推得更狠）；第 4/5 课不配 self_write。均按仓库约定带 `require_*` 即时校验。已用 venv（`/Users/bytedance/bob_code/llm-learning/.venv`，torch 2.8）逐一验证：3 个解答版端到端 exit 0、3 个留白版触发友好 ValidationError、4 主课 + kv demo 全 exit 0。（注：系统 python3 是坏的 3.9 且 `/tmp/inspect.py` 会污染 import，跑脚本要用 venv 且别在 /tmp 下跑。）
- 2026-06-24：新增 **PyTorch 专项训练**（`pytorch-essentials/`，phase2→phase3 的桥梁课，共 8 课）。缘起：phase0–2 全程 NumPy 手写、phase2 第 5 课才第一次用 `nn.Module`，而 phase3 第 1 课一上来就默认 DataLoader/AMP/梯度累积/checkpoint 都会——中间缺一层「PyTorch 本身的机制」。专项的学习哲学是「同一个东西先回顾 NumPy 手写版、再看 PyTorch 怎么做、再对拍验证数值一致」。8 课均按全仓库「三件套」约定搭建（主课讲解+可跑 / 留白 self_write 含 `require_*` 校验 / reset 重置脚本）：① `01_tensor_basics` view-reshape-stride-contiguous-dtype-device-↔numpy；② `02_autograd`（⭐）计算图/backward/detach，autograd 梯度与手写解析梯度+数值梯度三方对拍；③ `03_nn_module` Parameter vs buffer、state_dict、register_buffer、train/eval、参数统计/冻结；④ `04_loss_and_optim` CrossEntropy 与 logits、AdamW、param groups（矩阵权重 decay / bias-LayerNorm 不 decay）、warmup+cosine 调度（输出 `lr_schedule.png`）；⑤ `05_data_pipeline` Dataset/DataLoader/collate_fn 变长 pad、random_split、对比手写 get_batch；⑥ `06_training_loop`（⭐）五步曲+梯度裁剪/累积、eval(no_grad)、early stopping、checkpoint 断点续训、AMP；⑦ `07_debug_profile_memory` 三类经典报错+nan/inf 检测+显存（每参数 16 字节）+算力（C≈6ND，折算卡天）+profiler；⑧ `08_capstone_train`（🎓 毕业项目）把 8 课零件按真实工程顺序拼成 nanoGPT 风格脚本，训练迷你 token 语言模型并自回归生成验收。已验证：8 课主课全部 `exit 0`、8 个 reset 脚本执行成功且留白 self_write 均能编译、新增 `pytorch-essentials/README.md`，并同步更新根 [README.md](README.md)（结构树 / 路线图 / 课程总览 / 当前状态）。AI 协助搭建，全部 TODO 留白待亲手填。
- 2026-06-24：评估并补充 **ZeRO（Rajbhandari et al., 2020《ZeRO: Memory Optimizations Toward Training Trillion Parameter Models》**，arXiv:1910.02054，SC20）——微软 DeepSpeed 里 ZeRO 的奠基论文，PyTorch FSDP 的鼻祖。缘起：被问到「distributed optimiser state（分布式优化器状态）」这篇是否值得读。结论：值得读、定位为**概念精读 + 看图**（系统/分布式工程，不适合像 Attention 那样从零手搓），且正好补上项目里**多卡分布式训练**这块空白（phase3 现有 1-5 课全是单卡技巧），并能把第一阶段第 4 课算过的「每参数 ~16 字节显存账」从单卡视角推到集群视角（ZeRO 的全部出发点就是「优化器状态最吃显存，那就沿 N 卡切成 1/N」）。新增/修改：（1）下载 PDF 到 [papers/distributed-training/ZeRO_Memory_Optimizations_Toward_Training_Trillion_Parameter_Models_2020.pdf](papers/distributed-training/ZeRO_Memory_Optimizations_Toward_Training_Trillion_Parameter_Models_2020.pdf)，新建 `distributed-training/` 分类；（2）在 [papers/README.md](papers/README.md) 新增「分布式训练 / 显存切分」专题（含 ZeRO-1/2/3 三段切分读法、与 DP/TP/PP 关系、串到 phase1 第4课显存账与 `llm-interview-questions.md` 的 ZeRO 对照表）；（3）在 [phase3-training/README.md](phase3-training/README.md) 课表加「第 6 课 分布式训练专题（待补）」一行 + 大纲小节（DP/TP/PP、ZeRO、FSDP）；（4）同步更新根 [README.md](README.md) 结构树（新增 `distributed-training/` 目录）与论文清单表。AI 协助整理，论文待亲手精读，phase3 分布式训练课程待补。
- 2026-06-23：补充**检索增强 / RAG 三件套**（缘起：想系统学一下 RAG 的源头）。这三篇是检索增强这条线的奠基之作，自成一条独立脉络，现有分类都不贴，故新建 `papers/retrieval-augmented/` 分类。新增/修改：（1）下载三篇 PDF——**REALM**（Guu et al. 2020, arXiv:2002.08909, 检索进预训练）、**RAG**（Lewis et al. 2020, arXiv:2005.11401, 检索进生成、"RAG"名字出处）、**RETRO**（Borgeaud et al. 2021, arXiv:2112.04426, 检索 scale 到万亿 token + chunked cross-attention），均已核对页数完整（12/19/43 页）；（2）新增精读骨架 [papers/notes/retrieval_augmented_rag_notes.md](papers/notes/retrieval_augmented_rag_notes.md)（三篇合一、按「检索什么 / 怎么检索 / 怎么喂给模型」三个共性问题组织，含三篇对照表与「参数化知识 vs 非参数化知识」连接，正文留白待亲手精读后回填）；（3）在 [papers/README.md](papers/README.md) 目录分类新增 `retrieval-augmented/`，并加「12. 检索增强生成 / RAG」专题，给出 REALM→RAG→RETRO 推荐阅读顺序；（4）同步更新根 [README.md](README.md) 结构树与核心论文表。建议阅读顺序：REALM（检索进预训练）→ RAG（检索进生成）→ RETRO（检索 scale 到万亿 token），和 DeepSeek「稀疏架构 + KV 压缩」形成「参数化 vs 非参数化知识」的互补视角。AI 协助整理，三篇待亲手精读。
- 2026-06-22：第 6 课（Scaling Law）正式开学——基本读完 **Kaplan et al. 2020《Scaling Laws for Neural Language Models》**（Abstract / §1 Introduction / §3.1 三条幂律 Figure 1），建立起核心直觉：验证 loss 随参数量 N、数据量 D、训练 compute C 呈平滑幂律下降、可外推可预测，这是「敢花几亿美金闭眼训超大模型」的底气。对应精读笔记 [papers/notes/scaling_laws_kaplan_2020.md](papers/notes/scaling_laws_kaplan_2020.md)。下一步：读 Chinchilla 论文（1:20 修正 Kaplan）+ 跑通/吃透主课脚本 [phase2-transformer/06_scaling_laws.py](phase2-transformer/06_scaling_laws.py)。
- 2026-06-22：修正第 6 课资料里的两个小问题。（1）订正 [phase2-transformer/README.md](phase2-transformer/README.md) 第 6 课视频学习计划里的时间标注——Karpathy *Intro to Large Language Models* 讲 Scaling Law 的那一页 slide 实际在 `25:43-27:43`（官方章节 "LLM Scaling Laws"），原标的 `21:00-26:00` 串位了（`21:05-25:43` 那段其实是模型对比 / RLHF / 排行榜），已改正并加备注；（2）修复 [phase2-transformer/06_scaling_laws.py](phase2-transformer/06_scaling_laws.py) 两个 bug：图片输出路径原来写死成相对仓库根目录的 `phase2-transformer/xxx.png`，在 `phase2-transformer/` 目录里跑会 `FileNotFoundError`，改成用 `__file__` 锚定脚本自身目录（`SCRIPT_DIR + os.path.join`），任意 cwd 都能正确保存；并补上仓库约定缺失的 `sys.stdout.reconfigure(encoding="utf-8")`，解决 GBK 终端下中文输出的 `UnicodeEncodeError`。脚本现已 exit 0 跑通、两张幂律图正常保存。
- 2026-06-22：补充 **Anderson 1972《More Is Different》**（*Science* Vol. 177, No. 4047, pp. 393–396）——「涌现 / emergence」概念的思想源头，正是 Kaplan scaling laws 借用「规模累积可能涌现新能力」精神的出处。这是一篇跨学科的物理/科学哲学短文（非 arXiv），故新建 `papers/foundations/` 分类专放此类「思想源头」论文。新增/修改：（1）下载 PDF 到 [papers/foundations/More_Is_Different_Anderson_1972.pdf](papers/foundations/More_Is_Different_Anderson_1972.pdf)；（2）新增精读笔记 [papers/notes/more_is_different_anderson_1972.md](papers/notes/more_is_different_anderson_1972.md)，讲清还原论（接受）≠ 构成论（反对）、破缺对称性那条由小到大的链（氨分子不破→糖破→晶体宏观破→超导最深，30 年才解释）、科学层级「X 不是被应用的 Y」、马克思「量变→质变」与「富人只是钱更多」的金句，并专设一节把「量变（Kaplan 平滑幂律）↔ 质变（Anderson 涌现）」对照、串到 emergent abilities 争论；（3）在 [papers/README.md](papers/README.md) 目录分类新增 `foundations/`，并在 scaling-laws 阅读路径后加「Scaling Law 的思想源头（跨学科）」小节收录本篇；（4）同步更新根 [README.md](README.md) 结构树（新增 `foundations/` 目录）与论文清单表（加 More Is Different 一行）。AI 协助整理，论文待亲手精读。
- 2026-06-18：围绕 **KV Cache 在推理中的作用**做了一轮专题补强（缘起：被人问到 KV Cache 的作用）。（1）论文：新建 [papers/kv-cache/](papers/) 分类，下载 4 篇 KV Cache 推理优化经典——MQA《Fast Transformer Decoding》(1911.02150)、GQA(2305.13245)、PagedAttention/vLLM(2309.06180)、FlashAttention(2205.14135)，并在 [papers/README.md](papers/README.md) 新增「11. KV Cache 推理优化」专题与推荐阅读顺序（MQA → GQA → PagedAttention → FlashAttention → 回到 DeepSeek-V2 的 MLA），关联已有的 `Transformers are RNNs`（线性注意力＝用固定状态替代无限增长的 KV Cache）。（2）代码：新增纯 NumPy 最小演示 [phase3-training/kv_cache_numpy_demo.py](phase3-training/kv_cache_numpy_demo.py)，作为 PyTorch 版第 5 课 `05_inference_optimization.py` 的轻量对照版——Part 1 用同一套权重对比「无缓存整段重算 vs 有缓存逐 token」，验证两者输出最大差异仅 `2e-15`（缓存只省计算不改结果）；Part 2 统计 K/V 投影次数 `O(n²) vs O(n)`（P=10、G=50 时 1775→60，省 29.6x）；Part 3 用公式估算 GPT-2/LLaMA-7B/70B 的 KV Cache 显存。关键领悟：有缓存的 decode 步**天然不需要 causal mask**，因为缓存里只有「过去＋自己」，看不到未来。确认 KV Cache 早已在学习路径中（phase3 第 5 课正文 + phase4 第 3/4/5 课 MLA/FlashAttention/PagedAttention + phase5 第 8 课 KV cache 复用），本轮是补论文与 NumPy 对照 demo。AI 协助整理，论文待亲手精读。
- 2026-06-15：补充 **McCandlish et al. 2018《An Empirical Model of Large-Batch Training》**（arXiv:1812.06162）——Kaplan scaling laws §5.3 critical batch size 的源头论文，作者班底（McCandlish/Kaplan/Amodei）与 scaling laws 同源。新增/修改：（1）下载 PDF 到 [papers/scaling-laws/An_Empirical_Model_of_Large_Batch_Training_2018.pdf](papers/scaling-laws/An_Empirical_Model_of_Large_Batch_Training_2018.pdf)；（2）新增精读笔记 [papers/notes/large_batch_training_mccandlish_2018.md](papers/notes/large_batch_training_mccandlish_2018.md)，讲清「步数 vs 样本数」双曲线、critical batch size `B_crit = E_min/S_min`、gradient noise scale `B_noise`（信噪比 `tr(Σ)/|G|²`、可测且 `≈ B_crit`）、`B_noise` 随训练推进/任务难度增大（对应 GPT-3 batch 0.5M→3.2M warmup），并把它与 Kaplan/Chinchilla 三篇串成「batch / N-D-C / N:D」三条 scaling 轴；（3）在 [papers/notes/scaling_laws_kaplan_2020.md](papers/notes/scaling_laws_kaplan_2020.md) §5.3 补交叉引用到 PDF 与新笔记；（4）在第三阶段 [phase3-training/01_training_pipeline.py](phase3-training/01_training_pipeline.py) Part 5（梯度累积）补一段「梯度累积的目标是凑到 B_crit 附近、不是越大越好」的讲解并指向论文；（5）同步更新 [papers/README.md](papers/README.md) scaling-laws 阅读路径与根 [README.md](README.md) 论文清单。AI 协助整理，论文待亲手精读。
- 2026-06-15：在第一阶段新增**第 4 课「优化器」**，补上"梯度下降 → 真实训练/显存"之间一直缺的一环（第 2 课只学了朴素 SGD、phase3 直接把 `torch.optim.AdamW` 当黑盒）。新增三件套：（1）[phase1-foundations/04_optimizers.py](phase1-foundations/04_optimizers.py)：用一个条件数=100 的各向异性碗，从零 NumPy 实现并对比 SGD（260 步）/Momentum（64 步）/RMSprop（287 步，并说明它对 lr 敏感、本为带噪声场景设计）/Adam（98 步），讲清动量、自适应学习率、偏差修正、AdamW 解耦权重衰减，并算了 GPT-2/7B/70B 仅 Adam 状态(m+v)的显存（0.9/52/521 GB）+「每参数约 16 字节」拆解 + Adafactor 因式分解省内存原理，输出收敛曲线图；（2）[phase1-foundations/04_optimizers_self_write.py](phase1-foundations/04_optimizers_self_write.py)：6 个 TODO（sgd_step→momentum_step→rmsprop_step→adam_step→adamw_step→adam_training_bytes_per_param），单步更新函数设计成纯函数，校验用固定梯度序列对拍参考实现、并检查 weight_decay=0 时 AdamW≡Adam、端到端把碗优化下去；（3）[phase1-foundations/reset_exercises_04.py](phase1-foundations/reset_exercises_04.py)：已验证幂等（对留白版字节不变）、填满→重置可字节级还原。同步更新 phase1 README（课表/重置/优化器速查表）、根 README（结构树/路线图/课表/当前状态）。AI 协助搭建，6 个 TODO 全部留白待自己填。
- 2026-04-14：完成第一阶段第 3 课（神经网络前向/反向传播与 XOR 分类）自写练习，并已重置练习文件准备二刷。
- 2026-04-18：完成第二阶段第 1 课（词嵌入与位置编码）。跑通 `01_word_embeddings.py`、吃透正弦位置编码公式与 `div_term` 的 `exp(log)` 写法，看懂位置编码热力图/曲线图，完成自写练习 `01_word_embeddings_self_write.py`，并在 [phase2-transformer/README.md](phase2-transformer/README.md) 沉淀了"相加不是无损拼接，而是低成本注入顺序信息"和"放大镜 + 望远镜（小维度高频看近处、大维度低频看远处）"等 6 点关键领悟。可用 `reset_exercises_01.py` 重置练习准备二刷。
- 2026-04-18：补充 GPT-1 论文 PDF（`papers/core-transformers/GPT1_2018_improving_language_understanding.pdf`），并整理 [papers/notes/notes_gpt2_input_and_model.md](papers/notes/notes_gpt2_input_and_model.md) 笔记，配合第 1 课对照理解 decoder-only 的输入表示。
- 2026-04-25：完成第二阶段第 2 课（自注意力机制）。跑通 `02_self_attention.py` 并按 8 个 TODO 手写 `02_self_attention_self_write.py`：数值稳定 softmax -> Q/K/V 投影 -> scores -> scale -> softmax -> 加权 -> `self_attention` 函数封装 -> 因果掩码 `np.triu` -> `causal_self_attention`（GPT 用），全部校验通过。
- 2026-04-25：开始第二阶段第 3 课（多头注意力 / 残差连接 / LayerNorm）。在 [phase2-transformer/README.md](phase2-transformer/README.md) 补充了李宏毅 Multi-Head Attention 中文视频，按"看视频 -> 读论文 -> 跑 `03_multi_head_attention.py` -> 改 `n_heads`/去残差实验"的节奏推进。
- 2026-04-26：开始第 0 阶段第 1 课（向量、形状、axis、广播）。把 `01_vectors_and_axes.py` 的 axis 注释改成"聚合方向法"，给广播规则补了 5 条 Q&A（为什么从右对齐 / 为什么补 1 / 为什么不静默通过 / 不真复制内存 / 任意维度都成立），修正错误示范并加 `(3,4)` 与 `reshape(2,1,4)` 两个正确示范，给 `X+bias` 例子加 seed 和 diff 验证证明 bias 在 batch/seq 上共享。
- 2026-04-27：第 0 阶段第 1 课自写练习推进。在 `01_vectors_and_axes_self_write.py` 新增 TODO-0（动手验证 `(3,)` / `(1,3)` / `(3,1)` 的差异，练习 reshape 与 `None`/`np.newaxis` 两种写法）；在 TODO-4 后加 4a/4b/4c（3D 张量上的 axis、`keepdims` 与多轴聚合 `axis=(0,1)`、`mean`/`max`/`argmax` 共用 axis 规则）；在 TODO-7 `can_broadcast` 用例里补 6 个 5 维 case（含 attention scores + mask 的真实形状）。
- 2026-05-01：推进第二阶段第 3 课论文精读，完善 [papers/notes/attention_is_all_you_need_reading_3.1_3.2.2.md](papers/notes/attention_is_all_you_need_reading_3.1_3.2.2.md)：补清 FFN/MLP/position-wise 的术语关系、两层 FFN 中 `W_1/b_1` + ReLU + `W_2/b_2` 的结构、残差连接与残差相加的区别、encoder/decoder `N=6` 是实验超参数和对称设计而非硬约束、cross-attention 的 Q/K/V 来源与 mask 差异、causal mask 中 `✓/✗` 与 `0/1` 约定的区别、multi-head 中 concat/拼接、head 随机初始化打破对称性，以及 `W^O` 是可学习输出投影并负责混合多个 head。
- 2026-05-01：新增 [papers/notes/gpt3_reading_2.1_model_and_architectures.md](papers/notes/gpt3_reading_2.1_model_and_architectures.md)，精读 GPT-3 `2.1 Model and Architectures`：梳理 GPT-3 与 GPT-2 架构关系、dense / locally banded sparse attention 交替、8 个模型尺寸与 Table 2.1 读表注意、`d_model / d_ff / d_head` 的含义、Scaling Law 中验证 loss 与"暴力出奇迹"的关系，以及学习率、batch size、300B tokens 和 Chinchilla 修正口径。
- 2026-05-01：补充 Scaling Law 与 Sparse Transformer 延伸阅读资料。在 `papers/scaling-laws/` 下载 Kaplan et al. *Scaling Laws for Neural Language Models* 与 Chinchilla *Training Compute-Optimal Large Language Models*；在 `papers/efficient-transformers/` 下载 Child et al. *Generating Long Sequences with Sparse Transformers*；同步更新 [papers/README.md](papers/README.md) 和 [phase2-transformer/README.md](phase2-transformer/README.md)，把这些资料加入后续学习计划。
- 2026-05-01：新增 [papers/notes/bert_reading_model_architecture.md](papers/notes/bert_reading_model_architecture.md)，精读 BERT `§3` 开篇、`Model Architecture` 与 `Input/Output Representations` 架构衔接段：补清预训练/微调范式、MLM 与 GPT left-to-right LM 的差异、`L/H/A/4H` 记号、`BERT_BASE / BERT_LARGE` 尺寸、双向 self-attention 与 causal mask 的区别、WordPiece / `[CLS]` / `[SEP]` / segment embedding，以及与 `03_multi_head_attention.py` 五个 Part 的对照。
- 2026-05-01：补齐第二阶段第 3 课自写练习 [phase2-transformer/03_multi_head_attention_self_write.py](phase2-transformer/03_multi_head_attention_self_write.py)：按 9 个 TODO 手写数值稳定 softmax、单头 attention、`split_heads` / `merge_heads`、完整 `multi_head_attention`、所有 head 共享 causal mask、残差连接、LayerNorm、Post-Norm / Pre-Norm，并通过内置校验。
- 2026-05-02：完成第二阶段第 3 课（多头注意力 / 残差连接 / LayerNorm / Pre-Norm vs Post-Norm）。跑通并复盘 [phase2-transformer/03_multi_head_attention.py](phase2-transformer/03_multi_head_attention.py)：补清 `reshape` + `transpose(1, 0, 2)` 的维度重排、每个 head 独立 attention 后先 `append` 收集再 `concatenate(axis=-1)` 拼回 `d_model`、LayerNorm 中 `var` 按每个 token 的特征维计算，以及 `eps` 防止分母为 0 或过小。下一步从第 4 课 [phase2-transformer/04_transformer_block.py](phase2-transformer/04_transformer_block.py) 开始，把 Attention、残差、LayerNorm 和 FFN 组合成完整 Transformer Block。
- 2026-05-25：在第二阶段新增**第 6 课「Scaling Law 与 Compute-Optimal 训练」**，补全项目里一直缺的 Scaling Law 章节。新增/修改：（1）[phase2-transformer/06_scaling_laws.py](phase2-transformer/06_scaling_laws.py)：Part 1 Kaplan 三条幂律 `L(N)/L(D)/L(C)`、Part 2 log-log 直线可视化、Part 3 `C ≈ 6ND` 与 `compute_optimal_chinchilla(C)`、Part 4 GPT-3/Chinchilla/Llama 3/Qwen2.5/DeepSeek-V3 在 N-D 平面对比、Part 5 三个手算复盘题；（2）[papers/notes/scaling_laws_kaplan_2020.md](papers/notes/scaling_laws_kaplan_2020.md)：Kaplan 2020 精读，讲清三条幂律 / `N` 为什么不含 embedding / loss 为什么用 nats / Kaplan 的 compute-optimal 结论（`N ↑ C^0.73`、`D ↑ C^0.27`）/ critical batch size；（3）[papers/notes/chinchilla_compute_optimal_2022.md](papers/notes/chinchilla_compute_optimal_2022.md)：Chinchilla 2022 精读，讲清三种 approach（固定 N 扫 D / IsoFLOP / 参数化拟合）如何一致指向 `N:D ≈ 1:20`、`C ≈ 6ND` 工程估算、Chinchilla vs Gopher 实战对比、Kaplan 错在哪（LR schedule + 固定步数），并加「Chinchilla 之后」一节覆盖 Llama 3 over-training（推理成本视角）、DeepSeek-V3 / Qwen2.5 实际选择、测试时 compute 第四变量；（4）同步更新 [phase2-transformer/README.md](phase2-transformer/README.md)（第 6 课大纲：视频 / 论文 / 代码 / 手算题 / 5 点关键领悟）、[papers/README.md](papers/README.md)（两篇 PDF 从「未读」升级为第 6 课配套）、根 [README.md](README.md) 课表与论文清单。脚本跑通，输出 `scaling_laws_part2.png`、`scaling_laws_part4.png`。
- 2026-05-25：新增**第五阶段「Agent 架构与 Agent 基础设施」**（分支 `phase5-agent-architecture`），面向 Agent infra 方向打基础。定位「应用与 infra 均衡」+「课程地图 + 动手代码」，共 10 课：1 Agent Loop / 2 Tool Calling / 3 规划与反思 / 4 记忆 / 5 RAG 与上下文工程 / 6 Agent Runtime 与状态 / 7 工具沙箱与安全 / 8 并发调度与缓存复用 / 9 可观测性评测成本 / 10 Multi-Agent 编排与框架对比。新增：[phase5-agent-architecture/README.md](phase5-agent-architecture/README.md)（10 课大纲 + 学习节奏 + 必读材料 + 4 个实践项目 + 应用↔infra 对照表）、三个零依赖可运行脚本 `01_minimal_agent_loop.py`（最小 agent loop + 终止条件）、`02_tool_calling.py`（tool 注册表 + dispatcher + 错误回灌）、`03_agent_runtime.py`（run/step + checkpoint/resume + trace 成本报告，含「模拟崩溃→断点续跑」演示）。三个脚本均用规则驱动 `FakeLLM`，无需 GPU/网络/API key 即可跑通骨架。同步更新根 [README.md](README.md) 结构树、路线图与课程总览。
- 2026-05-30：学完第二阶段第 4 课（Transformer Block）主课。跑通并逐段读懂 [phase2-transformer/04_transformer_block.py](phase2-transformer/04_transformer_block.py)：FFN 两层 MLP（升维 4 倍 + GELU + 降维）与"存储知识"的直觉、`W_Q` 为何是 `(d_model, d_model)`（打包所有 head 一起算再 reshape 切分）、`reshape` + `transpose(1,0,2)` 的维度重排、`K.transpose(0,2,1)` + 批量矩阵乘法（保留 head 维当 batch、省 for 循环）、多头输出 concat 回 `d_model` 再过 `W_O`、残差连接（信息保底 + 梯度高速公路）、Pre-Norm Block 数据流、堆叠 6 层数值稳定（残差 + LayerNorm 的功劳）、Dropout（inverted dropout：训练丢弃并缩放 `1/(1-rate)`、推理原样返回）。给主课补充了大量"为什么"注释、修正了 Part 编号错位、新增 `relu_vs_gelu.png` 对比图（函数值 + 梯度）讲清为何用 GELU。把本课配套自写练习 [phase2-transformer/04_transformer_block_self_write.py](phase2-transformer/04_transformer_block_self_write.py) 合并为一份 9 个 TODO 的完整版（softmax → LayerNorm → GELU → FFN → multi_head_attention → Pre-Norm Block → Post-Norm Block → 堆叠 N 层 → inverted dropout，含内置 `require_*` 校验、参数量统计与真实模型规模对比），并在 docstring 补了 Pre-Norm 数据流图与核心公式。
- 2026-05-30：完成第二阶段第 4 课自写练习 [phase2-transformer/04_transformer_block_self_write.py](phase2-transformer/04_transformer_block_self_write.py)，9 个 TODO 全部手写并通过内置校验（数值稳定 softmax → LayerNorm → GELU → 两层 FFN → multi_head_attention → Pre-Norm Block → Post-Norm Block → 堆叠 6 层验证数值稳定 → inverted dropout）。本轮关键收获：（1）LayerNorm 第 211 行「减均值除标准差」与下游 `mean≈0 / std≈1` 校验的对应关系，以及 γ/β 缺省时才严格成立；（2）多头注意力踩中两个 numpy 转置坑——reshape 后必须再 `transpose` 把 `num_heads` 提到最前、`K.T` 会反转所有轴需改成 `K.transpose(0,2,1)` 只换后两维（批量矩阵乘法保留 head 维当 batch）；（3）`scores = Q·Kᵀ/√d_k` 的 Query-Key 点积语义与 √d_k 缩放防 softmax 过尖；（4）inverted dropout 训练时 `x*mask/(1-rate)` 用 `1/(1-rate)` 补偿期望、推理直接原样返回。校验输出含参数量统计（单 Block 3,152，FFN 占约 68%）与 GPT-2/GPT-3/LLaMA-2 真实规模对比。下一步进入第 5 课，把 Token Embedding + Position Encoding + N 层 Block + LM Head 拼成迷你 GPT。
- 2026-06-06：开始第二阶段第 5 课（从零搭建 GPT），第二阶段首次进入 PyTorch 环境。已完成：（1）安装 PyTorch（`torch 2.12.0+cpu`，Python 3.14）；（2）跑通 [phase2-transformer/05_gpt_from_scratch.py](phase2-transformer/05_gpt_from_scratch.py)，观察到 220,544 参数的迷你 GPT 训练 loss 从 3.46（≈`ln(31)`，随机瞎猜基线）一路降到 0.31，并生成莎士比亚风格文本，验证了 Temperature 0.5/1.0/1.5 的确定性↔随机性差异；（3）给该文件入口补 `sys.stdout.reconfigure(encoding="utf-8")` 修复 Windows 中文乱码（已提交并推送，commit `f37be02`）；（4）重读了《Attention Is All You Need》论文。**待完成（明天继续）**：① 在本地自己亲手跑一遍第 5 课看效果；② 精读 Part 2 的 `CausalSelfAttention`（把论文公式 `softmax(QKᵀ/√dₖ)V`、`√dₖ` 缩放、masked attention、多头 concat + `W^O` 与代码第 96～118 行逐行对应）；③ 继续读 `TransformerBlock`（残差 + Pre-Norm，146～149 行）与 Part 3 完整 `GPT` 类。
- 2026-06-14：**完成第二阶段第 5 课（从零搭建 GPT）自写练习**，12 个 TODO 全部亲手填完并通过内置校验，迷你 GPT（27,488 参数）真训 300 步 loss 从 3.43 → 0.43、生成出莎士比亚风格文本。本轮踩点与收获：（1）`split_qkv_heads` 用 `qkv.split(C, dim=2)` 先拆出 Q/K/V，再 `view(B,T,n_head,d_k).transpose(1,2)` 把 head 维提到前面；（2）`causal_attention` 用 `masked_fill(mask==1, -inf)` 屏蔽未来 token，最后 `transpose(1,2).contiguous().view(B,T,C)` 合并多头——`contiguous` 是 `transpose` 后 `view` 的必需步骤；（3）**weight tying** 用 `self.lm_head.weight = self.token_embedding.weight`（共享同一个 `Parameter` 对象，不是 `copy_` 数值拷贝），校验确认两者 `is` 同一对象；（4）`generate` 每步先 `idx[:, -context_len:]` 裁剪、取最后一个时刻 logits、除 temperature、可选 `apply_top_k`、`softmax` 后 `multinomial` 采样再 `cat` 拼回；（5）`apply_top_k` 取第 k 大值当阈值 `masked_fill` 掉更小的；`apply_top_p`（进阶）先排序累加概率、`remove` 右移一位保证至少留 top-1、再 `scatter` 还原到原始顺序；（6）`get_batch` 的 `y` 是 `x` 右移一位，正是 next-token prediction 的本质。另外今天给 TODO-12 的 BPE 演示补了三块体感打印（id↔切片对应、前导空格归属导致 `'be'`/`' be'` id 不同、常见词 1 token vs 生僻词/中文拆多个子词），加深对子词分词「为什么不用字符级」的理解。
- 2026-06-10：完善第 5 课学习设施（AI 协助，练习 TODO 全部留白待自己填）。（1）新增自写练习 [phase2-transformer/05_gpt_from_scratch_self_write.py](phase2-transformer/05_gpt_from_scratch_self_write.py)：10 个 TODO（因果掩码 → QKV 分头 → 注意力核心计算 → FFN → Pre-Norm Block → GPT.forward → get_batch → Top-K 过滤 → generate →（进阶可跳过）Top-P），各类 `__init__` 已给出、只写 forward；校验含「改未来 token 不影响过去 logits」的因果性测试和「top_k=1 等价贪心解码」检查，全部通过后脚本会用自己写的模型真训 300 步并生成文本。（2）修复主课 `05_gpt_from_scratch.py`：`torch.arange` 补 `device=idx.device`（GPU 兼容）、区分 Part 3 演示配置与 Part 4 实际训练模型的参数量打印、补 weight tying 与 GPT-2 残差初始化的「为什么」注释。（3）补齐第 2/3/4 课重置脚本并新增 `reset_exercises_05.py`（已在临时副本上验证幂等、重置后可运行、05 重置结果与原始留白版逐字节一致）。（4）给第 2/3/4 课自写练习补 `sys.stderr` UTF-8 reconfigure，修复 `ValidationError` 报错信息在 PowerShell 下乱码的问题。（5）同步 phase2 README 与根 README 的进度标记、课程表与文件清单。（6）同日补充：把主课练习区 6 道题全部融合进自写练习——新增 TODO-6 weight tying（自己写出「共享同一个 Parameter」的赋值并校验不是数值拷贝）与 TODO-12 tiktoken BPE 分词（编码 + 解码还原 + 对比字符级序列长度，可跳过），共 12 个 TODO；训练环节支持检测 `tiny_shakespeare.txt` 自动用真实数据、支持替换 `training_text` 玩中文/代码/歌词；主课练习区标注了每道题对应的 TODO 去处；另把 `block_size` 统一重命名为 `context_len`。下一步：按第 5 课节奏完成精读后亲手填 12 个 TODO。
